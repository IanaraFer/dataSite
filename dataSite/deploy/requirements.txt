# Vercel Deployment Dependencies - Minimal for Fast Builds
fastapi==0.104.1
stripe==7.8.0
sendgrid==6.11.0
python-multipart==0.0.6
pydantic[email]==2.5.0
python-dotenv==1.0.0
azure-keyvault-secrets==4.7.0

# Logging and Monitoring
structlog==23.2.0

# Additional ML Libraries (optional but recommended)
scipy==1.11.4
statsmodels==0.14.0

# Add this in the sidebar section after data loading:
if st.session_state.data_loaded:
    st.header("🤖 AI Analysis Options")
    
    analysis_type = st.selectbox(
        "Select Analysis Type",
        [
            "Business Overview",
            "Revenue Forecasting",
            "Customer Segmentation", 
            "Anomaly Detection",
            "Comparative Analysis"
        ]
    )
    
    if st.button("🚀 Run Analysis", use_container_width=True):
        st.session_state.selected_analysis = analysis_type
        st.rerun()
